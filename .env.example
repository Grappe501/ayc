# Server-only Postgres connection (never expose to the browser bundle)
DATABASE_URL=

# Shared leader write gate for Phase 1D+ (server-only)
AYC_LEADER_WRITE_SECRET=

AYC_ENVIRONMENT=development
AYC_SITE_NAME=AYC Leadership Workbench
AYC_ALLOWED_ORIGIN=
LOG_LEVEL=info

# After DATABASE_URL is set:
#   npm run db:migrate
#   npm run db:seed-teams
#   npm run db:seed-roster   # Chance Bradford board + leadership intake list
